const { async } = require("crypto-random-string");

module.exports = {
    exits : {
        success : {
            viewTemplatePath : 'account/compare'
        }
    },

    fn: async function() {
        return{}
    }
}